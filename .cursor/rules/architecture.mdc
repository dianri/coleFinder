---
description: Arquitectura general de ColeFinder
globs:
alwaysApply: true
---

# ColeFinder - Arquitectura

## Contexto del proyecto
- App Android nativa para docentes que geolocaliza centros educativos.
- Estado actual: MVP avanzado.
- Backend principal: Supabase alojado en https://api.dianrie.com.
- Lenguaje principal: Kotlin 2.0.21.
- Target SDK: 36.

## Reglas obligatorias
- Usar siempre Kotlin moderno.
- No usar XML salvo que el usuario lo pida explícitamente.
- La UI debe implementarse con Jetpack Compose y Material 3.
- Mantener arquitectura MVVM con flujo unidireccional de estado.
- Toda lógica de datos debe integrarse en `MapState`.
- El ViewModel principal es `MapViewModel`.
- Sin escrituras directas a Supabase desde composables ni desde el ViewModel:
  toda operación de red pasa por `ColegioRepository` (inyectado en el ViewModel).
- Evitar refactors grandes si no son necesarios para la tarea.
- Hacer cambios mínimos, coherentes y compilables.

---

## Capas de la arquitectura

```
MapScreen (Compose)
    └── MapViewModel (HiltViewModel)
            ├── ColegioRepository (interfaz)
            │       └── SupabaseColegioRepository (implementación, @Singleton)
            │               └── SupabaseClient (Hilt → NetworkModule)
            └── UserPreferencesRepository (@Singleton, DataStore)
```

### `ColegioRepository` (interfaz en `data/repository/`)
Contrato de acceso a datos de centros educativos. Permite sustituir la
implementación de Supabase por una de Room (modo offline) sin tocar el ViewModel.

```kotlin
interface ColegioRepository {
    suspend fun fetchNearbyColegios(
        lat: Double,
        lon: Double,
        limit: Int = 50,
        titularidades: Set<TitularidadFiltro> = setOf(TitularidadFiltro.TODOS),
        tipos: Set<TipoCentroFiltro> = setOf(TipoCentroFiltro.TODOS)
    ): Result<List<ColegioConDistancia>>

    suspend fun searchColegiosByName(
        query: String,
        limit: Int = 8
    ): Result<List<ColegioSearchDto>>
}
```

### `SupabaseColegioRepository` (`data/repository/`)
Implementación concreta de `ColegioRepository`:
- Ejecuta en `Dispatchers.IO`.
- `fetchNearbyColegios`: construye parámetros JSON con `buildJsonObject`, llama a
  `supabase.postgrest.rpc("nearby_colegios", params) { schema = BuildConfig.SUPABASE_SCHEMA }`.
  Aplica `.distinctBy { it.id }` antes de mapear DTO → dominio.
- `searchColegiosByName`: consulta PostgREST directa con `from(schema, "colegios").select { ilike("nombre", ...) }`.
  Devuelve `List<ColegioSearchDto>` (DTO ligero: id, nombre, localidad, lat, lon).
- Errores de `fetchNearbyColegios` se clasifican con `classifyColegiosLoadFailure()` (`data/network/ColegiosLoadError.kt`)
  y se devuelven como `Result.failure(ColegiosLoadException(...))` con categoría (DNS, TIMEOUT, SSL, etc.) y mensaje de usuario.
- Registrada en Hilt via `RepositoryModule` (`@Binds`).

### `UserPreferencesRepository` (`data/repository/`)
Persiste preferencias de UI mediante DataStore Preferences:
- `hasDiscoveredLongPress: Boolean` — si el usuario ya ha hecho long press en el mapa.
- `longPressHintCount: Int` — número de veces que se ha mostrado el hint.
- Métodos: `updateHasDiscoveredLongPress(Boolean)`, `incrementHintCount()`.
- Expone `userPreferencesFlow: Flow<UserPreferences>` al ViewModel.

---

## Estado y flujo

### `MapState` — esquema real actual

```kotlin
data class MapState(
    val isLoading: Boolean = false,
    val colegiosCercanos: List<ColegioConDistancia> = emptyList(),   // resultado bruto de la RPC
    val suggestions: List<SearchSuggestion> = emptyList(),
    val error: String? = null,
    val userLocation: LatLng? = null,
    val puntoReferencia: LatLng? = null,
    val selectedColegioConDistancia: ColegioConDistancia? = null,    // Selección completa para la card
    val filtrosTitularidad: Set<TitularidadFiltro> = setOf(TitularidadFiltro.TODOS),
    val filtrosTipoCentro: Set<TipoCentroFiltro> = setOf(TipoCentroFiltro.TODOS),
    val favoritosIds: Set<Int> = emptySet(),                         // Favoritos in-memory (Room es roadmap)
    val cameraPosition: CameraPositionState = ...,
    val showLongPressHint: Boolean = false,
    val hasDiscoveredLongPress: Boolean = false,                      // Sincronizado desde DataStore
    val longPressHintCount: Int = 0,                                  // Sincronizado desde DataStore
    val focusedRequestType: FocusedRequestType = FocusedRequestType.NONE,
    val showRemoteResultsWarning: Boolean = false
) {
    // Computed — NO es un campo del constructor:
    val selectedColegio: Colegio? get() = selectedColegioConDistancia?.colegio

    // Getter que aplica ambos filtros en cliente (fallback de coherencia visual):
    val colegiosConDistancia: List<ColegioConDistancia>
        get() = colegiosCercanos.filter { ... }
}
```

### `SearchSuggestion` (en `MapState.kt`)
```kotlin
data class SearchSuggestion(
    val title: String,
    val subtitle: String,
    val latLng: LatLng,
    val colegioId: Int? = null   // non-null → centro de BD; null → ubicación de Geocoder
)
```

### `FocusedRequestType` (enum en `MapState.kt`)
```kotlin
enum class FocusedRequestType { NONE, MY_LOCATION, POINT, SEARCH }
```
Controla si la cámara debe auto-ajustar el encuadre tras una carga.
El ViewModel lo activa en `updateUserLocation`, `onMapLongClick`, `selectSuggestion` y `buscarDireccion`.
La UI lo consume en un `LaunchedEffect` y llama a `viewModel.consumeFocusedRequest()` al terminar.

### `ColegioConDistancia`
```kotlin
data class ColegioConDistancia(
    val colegio: Colegio,
    val distanciaMetros: Double,
    val tipoCentroClasificado: TipoCentroClasificado,  // derivado de BD + fallback cliente
    val titularidadNormalizada: String?                 // PUBLICO / CONCERTADO / PRIVADO / null
)
```

---

## `MapViewModel` — responsabilidades reales

- `initializeMap(userLatLng: LatLng?)`: carga inicial única (flag `initialized`);
  usa ubicación del usuario o Madrid por defecto.
- `loadNearbyColegios(lat, lon)`: delega en `colegioRepository.fetchNearbyColegios(...)`.
  Pasa los filtros activos del estado actual. Actualiza `colegiosCercanos`.
- `reloadWithCurrentFilters()`: recarga la RPC con el punto de referencia actual
  (usa `puntoReferencia` o `userLocation` o Madrid por defecto).
- `toggleFiltroTitularidad(filtro)` / `toggleFiltroTipoCentro(filtro)`:
  actualizan sets de filtros **y recargan** vía `reloadWithCurrentFilters()`.
- `onSearchQueryChanged(query, context)`: búsqueda híbrida con debounce de 300 ms.
  Lanza en paralelo (`async`): Geocoder (ubicaciones) + `colegioRepository.searchColegiosByName` (centros por nombre).
  Fusiona resultados: colegios primero, luego ubicaciones.
- `selectSuggestion(suggestion)`: centra la cámara, fija `puntoReferencia` y recarga centros.
  Si `suggestion.colegioId != null`, fija `pendingColegioSelection` para auto-seleccionar
  el centro tras la carga de `loadNearbyColegios`.
- `updateUserLocation(latLng)`: centra la cámara en la ubicación del usuario, limpia `puntoReferencia` y recarga.
- `onMapLongClick(latLng)`: marca `puntoReferencia`, recarga centros, persiste descubrimiento.
- `onMapClick()`: limpia selección; muestra hint si el usuario aún no ha descubierto el long press.
- `onMapMoved()`: muestra hint si el usuario aún no ha descubierto el long press.
- `onHintDismissed()`: limpia `showLongPressHint`.
- `onColegioClick(ColegioConDistancia)`: actualiza `selectedColegioConDistancia` y anima cámara.
- `moverAColegio(latLng, ColegioConDistancia)`: solo cámara + selección, nunca recarga datos.
- `clearSelectedColegio()`: limpia `selectedColegioConDistancia`.
- `toggleFavorito(colegioId)`: alterna el id en `favoritosIds` (in-memory).
- `setMostrarAvisoCentrosLejanos(Boolean)`: activa/desactiva el aviso de centros lejanos.
- `clearError()`: limpia `state.error` tras mostrarlo en UI (evita toasts repetidos).
- `consumeFocusedRequest()`: resetea `focusedRequestType` a NONE tras consumir el auto-encuadre.
- `buscarDireccion(query, context)`: busca con Geocoder, centra cámara, fija `puntoReferencia` y recarga centros.

> **Nota sobre `focusOnCurrentLocationAndLoadNearby`**: esta función existe como
> lambda local dentro de `MapScreen` (no en el ViewModel). Usa `FusedLocationProviderClient`
> con fallback de `lastLocation` → `getCurrentLocation(PRIORITY_HIGH_ACCURACY)`.
> El ViewModel no tiene acceso directo al `FusedLocationProviderClient`.

---

## Archivos clave del dominio de mapa

| Archivo | Rol |
|---|---|
| `MapState.kt` | Estado UI + getter de filtrado + enums `FocusedRequestType` |
| `MapViewModel.kt` | Lógica de negocio, delegación a repositorios |
| `MapScreen.kt` | UI Compose principal + `MapSheetValue` enum + sheet custom |
| `CentroFiltros.kt` | Enums de filtros, clasificación de tipo, colores, toggle logic, arrays RPC |
| `NearbyColegioDto.kt` | DTO de deserialización de la RPC `nearby_colegios` |
| `ColegioSearchDto.kt` | DTO ligero para búsqueda por nombre (id, nombre, localidad, lat, lon) |
| `Colegio.kt` | Modelo de dominio + `JornadaTipo` enum |
| `ColegioRepository.kt` | Interfaz de acceso a datos |
| `SupabaseColegioRepository.kt` | Implementación Supabase de `ColegioRepository` |
| `UserPreferencesRepository.kt` | Persistencia de preferencias con DataStore |
| `ColegiosLoadError.kt` | Clasificación de errores de red: `ColegiosLoadCategory`, `ColegiosLoadException` |
| `NetworkModule.kt` / `RepositoryModule.kt` | Módulos Hilt (en `di/`) |

---

## Tests unitarios JVM (`app/src/test`)

Stack habitual: **JUnit 4**, **MockK** (`coEvery` / `coVerify` para suspend), **kotlinx-coroutines-test** (`runTest`, `StandardTestDispatcher` vía regla), **Turbine** (`state.test { }` sobre el `StateFlow` del ViewModel).

| Archivo | Rol |
|---|---|
| `es/colefinder/test/MainDispatcherRule.kt` | `@get:Rule` que sustituye `Dispatchers.Main` por un `TestDispatcher` (`StandardTestDispatcher()`). |
| `es/colefinder/ui/map/MapViewModelTest.kt` | ViewModel: favoritos, filtros + verificación de `fetchNearbyColegios`, errores, long click, selección, carga, `consumeFocusedRequest`. |
| `es/colefinder/ui/map/MapStateTest.kt` | Getter `MapState.colegiosConDistancia` (filtros titularidad/tipo, AND, vacío, fallback texto libre). |
| `es/colefinder/ui/map/CentroFiltrosTest.kt` | Clasificación de tipo, toggles de filtros, helpers RPC, matching de titularidad. |
| `es/colefinder/data/SupabaseHeaderTest.kt` | Smoke de cabeceras de esquema en RPC vía cliente Ktor mock. |

Convenciones útiles en `MapViewModelTest`:

- Stub de `fetchNearbyColegios` con **tercer argumento explícito `50`** (`limit` por defecto del ViewModel/repositorio) para que MockK coincida con la firma real.
- `UserPreferencesRepository`: `every { userPreferencesFlow } returns MutableStateFlow(UserPreferences(...))`.
- Para forzar `Result.failure` en carga cercana sin depender de stubs frágiles, usar una **implementación mínima** de `ColegioRepository` de prueba que devuelva el `Result` deseado.
- Tras tests que disparan trabajo asíncrono del ViewModel (p. ej. animación de cámara en `onColegioClick`), llamar a **`cancelForTest()`** (cancelación del `viewModelScope`) para evitar efectos colaterales al drenar la cola del test.

No existe plantilla `ExampleUnitTest`; la cobertura relevante está en los archivos anteriores.

---

## Deduplicación obligatoria
- Antes de actualizar el estado con listas de colegios, aplicar siempre:
  - `.distinctBy { it.id }`
- Este `.distinctBy` ocurre en `SupabaseColegioRepository`, no en el ViewModel.

## Flujo de filtros (servidor + cliente)
La RPC acepta `p_titularidades text[]` y `p_tipos text[]` (arrays opcionales).
Cuando hay opciones concretas seleccionadas, `SupabaseColegioRepository` construye
el array con `Set<TitularidadFiltro>.toRpcArray()` (en `CentroFiltros.kt`).
El getter `colegiosConDistancia` en `MapState` aplica ambos filtros en memoria
como **fallback de UI** para garantizar coherencia visual.
Cambiar un filtro **sí** provoca una nueva llamada a Supabase (vía `reloadWithCurrentFilters()`).
