package es.colefinder.data.model

/** Claves conocidas en `app_config` (key / value). */
object AppConfigKeys {
    const val MIN_VERSION_CODE = "min_version_code"
    const val UPDATE_TYPE = "update_type"
    const val NEARBY_COLEGIOS_LIMIT = "nearby_colegios_limit"
}
